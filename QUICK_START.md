# Quick Start Guide - دليل البدء السريع

## التثبيت والتشغيل الفوري

```bash
# 1. تثبيت الحزم المطلوبة
npm install

# 2. تشغيل خادم التطوير
npm run dev

# 3. فتح المتصفح على:
# الموقع الرئيسي: http://localhost:3000
# لوحة التحكم: http://localhost:3000/admin/login
```

## بيانات الدخول الافتراضية (Default Credentials)

- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin`

⚠️ **تحذير مهم**: قم بتغيير كلمة المرور فوراً من لوحة التحكم قبل النشر في الإنتاج!

## الميزات الجاهزة للاستخدام

### الصفحات العامة (Public Pages)
- ✅ **الرئيسية** (`/`) - مع بطل أخاذ وكاروسيل التقنيات المتحركة
- ✅ **صفحة المشاريع** (`/projects`) - مع Pagination و "تحميل المزيد"
- ✅ **صفحة التواصل** (`/contact`) - مع نموذج اتصال مباشر

### لوحة التحكم (Admin Dashboard) 
متاحة على `/admin` بعد تسجيل الدخول:

#### 1. **إدارة المشاريع** (Projects)
- إضافة مشاريع جديدة مع صور
- تحديث بيانات المشاريع
- حذف المشاريع
- تعيين شارات لكل مشروع
- إضافة روابط المشاريع

#### 2. **إدارة التقنيات** (Technologies)
- إضافة تقنيات جديدة
- تعديل التقنيات الموجودة
- حذف التقنيات
- **ملاحظة**: تظهر تلقائياً في الكاروسيل المتحرك على الصفحة الرئيسية

#### 3. **إدارة الشارات** (Badges)
- إضافة شارات جديدة
- اختيار ألوان مخصصة لكل شارة
- تعديل وحذف الشارات

#### 4. **رسائل التواصل** (Messages)
- عرض جميع رسائل العملاء المرسلة
- عرض تفاصيل كل رسالة
- حذف الرسائل

#### 5. **الإعدادات** (Settings)
- تغيير نص زر "احجز استشارة"
- تغيير رابط الزر (يمكن أن يكون صفحة داخلية أو خارجية)

#### 6. **تغيير كلمة المرور** (Change Password)
- تغيير كلمة مرور المسؤول
- يتطلب كلمة المرور الحالية للتأكيد

## هيكل قاعدة البيانات

يتم إنشاء قاعدة البيانات تلقائياً في `portfolio.db` عند أول تشغيل:

```
users               → بيانات المسؤولين
projects            → المشاريع والمحتوى
technologies        → التقنيات المستخدمة
badges              → الشارات والميزات
project_badges      → ربط المشاريع بالشارات
contact_messages    → رسائل التواصل من العملاء
button_settings     → إعدادات الأزرار
```

## التخصيص والتطوير

### تغيير الألوان والتصميم
- **الملف الرئيسي للأنماط**: `app/globals.css`
- **إضافة ألوان مخصصة**: في `tailwind.config` (عند الحاجة)
- **اللون الأساسي الحالي**: `#2b8cee` (أزرق)

### إضافة ميزات جديدة
1. **مكونات جديدة**: أضفها في مجلد `components/`
2. **صفحات جديدة**: أنشئ مجلد جديد في `app/`
3. **API جديد**: أضفه في `app/api/`

### ملفات مهمة
- `lib/db.ts` - إدارة قاعدة البيانات
- `lib/auth.ts` - دوال المصادقة
- `middleware.ts` - حماية المسارات

## النشر في الإنتاج

قبل النشر، قم بـ:

1. ✅ تغيير كلمة المرور الافتراضية
2. ✅ تحديث `JWT_SECRET` في `.env.local`
3. ✅ مراجعة إعدادات الأمان
4. ✅ إعداد نسخ احتياطية لقاعدة البيانات
5. ✅ تفعيل HTTPS

```bash
npm run build
npm start
```

## المشاكل الشائعة والحلول

**المشكلة**: لا يمكن تسجيل الدخول
**الحل**: تأكد من استخدام `admin` و `admin` كبيانات افتراضية

**المشكلة**: الصور لا تظهر
**الحل**: استخدم رابط URL كامل للصور (مثل: https://example.com/image.jpg)

**المشكلة**: الكاروسيل لا يتحرك
**الحل**: تأكد من أن لديك على الأقل 3 تقنيات مضافة

## الدعم والموارد

- **الوثائق الكاملة**: اقرأ `README_AR.md` للتفاصيل الكاملة
- **مستندات Next.js**: https://nextjs.org/docs
- **Tailwind CSS**: https://tailwindcss.com/docs

---

**استمتع ببناء معرض أعمالك! 🚀**
